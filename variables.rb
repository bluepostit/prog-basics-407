age = 32

puts "My age is #{age}"
puts "My age is #{age + 1}"

# `age` is unchanged - because we did not ASSIGN it a new value.
puts "What's my age again? #{age}"

# Assigning a new value to `age`.
age = age + 1
puts "I'm now a year older. My age is #{age}"
